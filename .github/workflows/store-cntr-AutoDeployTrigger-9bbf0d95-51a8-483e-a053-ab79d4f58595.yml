name: Trigger auto deployment for store-cntr

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/store-cntr-AutoDeployTrigger-9bbf0d95-51a8-483e-a053-ab79d4f58595.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.STORECNTR_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.STORECNTR_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.STORECNTR_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.STORECNTR_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.STORECNTR_REGISTRY_PASSWORD }}
          containerAppName: store-cntr
          resourceGroup: Store-RG
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


